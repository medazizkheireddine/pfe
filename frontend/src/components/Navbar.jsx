// src/components/Navbar.jsx
import React from 'react';

const Navbar = () => (
    <nav className="navbar">
        <h2>IT Management App</h2>
        <button>Logout</button>
    </nav>
);

export default Navbar;
